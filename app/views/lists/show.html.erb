<h1>Lists#show</h1>
<p>Find me in app/views/lists/show.html.erb</p>

<h2><%= @list.name %></h2>

<%# <% @list.movies.each do |movie| %>
<%# bookmark = Bookmark.find_by(list: @list, movie: movie) %>
<%#= movie.title %>
<%#= bookmark.comment %>
<%# end %>

<br>

<% @bookmarks.each do |bookmark| %>
<li><%= bookmark.movie.title %></li>
<ul>
  <li><%= bookmark.movie.rating %></li>
  <li><%= bookmark.movie.overview %></li>
  <li><%= bookmark.comment %> (<%= link_to "Delete this bookmark",
      bookmark_path(bookmark),
      data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
    %>)</li>
</ul>
<% end %>

<br>

<!-- <p><%= link_to "Create a bookmark", new_list_bookmark_path(@list) %></p> -->
<!-- <br> -->

<p>Add a movie below...</p>
<%= render "bookmarks/form" %>

<br>

<%= link_to "Delete this list",
      list_path(@list),
      data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
    %>

<br>

<p><%= link_to "Back to lists", lists_path %></p>
